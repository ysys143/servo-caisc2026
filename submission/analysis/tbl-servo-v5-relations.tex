% AUTO-GENERATED by analysis/build_servo_v5_tables.py. Do not edit by hand.
%
% Table B2 -- Relation / evidence (per functional relation), a longtable so it
% breaks across pages. \caption and \label{tab:servo-v5-relations} are
% embedded in the table itself (T11/G4) -- place directly with
% \input{analysis/tbl-servo-v5-relations.tex}, no table*/tabular wrapper
% needed. Caption text:
%
% Relation/evidence table (Table B2): each row is one distinct (source support, occurrence resolution, evidence mode) combination carried by a typed (source role, relation type, target role, temporal scope) relation, grouped by case, so the three axes on a row correspond; a relation with several combinations spans several rows with its label repeated. Source support, occurrence resolution, and evidence mode are kept separate. Evidence mode names the kind of proposition the source supports and is not an ordinal scale of system quality or closure.
\begin{longtable}{l p{0.34\linewidth} p{0.15\linewidth} p{0.15\linewidth} p{0.16\linewidth}}
\caption{Relation/evidence table (Table B2): each row is one distinct (source support, occurrence resolution, evidence mode) combination carried by a typed (source role, relation type, target role, temporal scope) relation, grouped by case, so the three axes on a row correspond; a relation with several combinations spans several rows with its label repeated. Source support, occurrence resolution, and evidence mode are kept separate. Evidence mode names the kind of proposition the source supports and is not an ordinal scale of system quality or closure.}\label{tab:servo-v5-relations} \\
\toprule
Case & Functional relation & Source support & Occurrence resolution & Evidence mode \\
\midrule
\endfirsthead
\multicolumn{5}{l}{\emph{Table B2 (continued)}} \\
\toprule
Case & Functional relation & Source support & Occurrence resolution & Evidence mode \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\emph{continued on next page}} \\
\endfoot
\bottomrule
\endlastfoot
C01 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{environment} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{unresolved} & \texttt{occurrence} \\
C01 & \texttt{environment} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C01 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{evaluation} $\to$ \texttt{revises} $\to$ \texttt{artifact} (\texttt{cross\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{external} $\to$ \texttt{selects} $\to$ \texttt{action} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C01 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{memory} $\to$ \texttt{conditions} $\to$ \texttt{policy} (\texttt{cross\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C01 & \texttt{observation} $\to$ \texttt{updates} $\to$ \texttt{inquiry\_state} (\texttt{cross\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C01 & \texttt{policy} $\to$ \texttt{selects} $\to$ \texttt{action} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C01 & \texttt{policy} $\to$ \texttt{selects} $\to$ \texttt{action} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
\midrule
C02 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C02 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C02 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C02 & \texttt{evaluation} $\to$ \texttt{triggers} $\to$ \texttt{execution} (\texttt{cross\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C02 & \texttt{execution} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C02 & \texttt{execution} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C02 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C02 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C02 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{contradicted} & \texttt{n.a.} & \texttt{aggregate} \\
C02 & \texttt{memory} $\to$ \texttt{reads} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{memory} $\to$ \texttt{reads} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C02 & \texttt{observation} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C02 & \texttt{observation} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
\midrule
C03 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{action} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{environment} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C03 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C03 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C03 & \texttt{memory} $\to$ \texttt{reads} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{evaluation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C03 & \texttt{policy} $\to$ \texttt{selects} $\to$ \texttt{action} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
\midrule
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C04 & \texttt{execution} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C04 & \texttt{execution} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C04 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{cross\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C04 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C04 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C04 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C04 & \texttt{memory} $\to$ \texttt{reads} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{memory} $\to$ \texttt{writes} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{observation} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C04 & \texttt{policy} $\to$ \texttt{selects} $\to$ \texttt{action} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
\midrule
C05 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C05 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C05 & \texttt{environment} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C05 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C05 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C05 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C05 & \texttt{evaluation} $\to$ \texttt{updates} $\to$ \texttt{inquiry\_state} (\texttt{cross\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{occurrence} \\
C05 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C05 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C05 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C05 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C05 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
\midrule
C06 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C06 & \texttt{action} $\to$ \texttt{produces} $\to$ \texttt{execution} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{environment} $\to$ \texttt{produces} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C06 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C06 & \texttt{evaluation} $\to$ \texttt{evaluates} $\to$ \texttt{observation} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{execution} $\to$ \texttt{reads} $\to$ \texttt{external} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{external} $\to$ \texttt{conditions} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C06 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{artifact} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C06 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C06 & \texttt{external} $\to$ \texttt{evaluates} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C06 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{aggregate} \\
C06 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
C06 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{supported} & \texttt{n.a.} & \texttt{procedure} \\
C06 & \texttt{generation} $\to$ \texttt{produces} $\to$ \texttt{candidate} (\texttt{per\_step}) & \texttt{unresolved} & \texttt{unresolved} & \texttt{capability} \\
C06 & \texttt{generation} $\to$ \texttt{revises} $\to$ \texttt{candidate} (\texttt{cross\_step}) & \texttt{supported} & \texttt{resolved} & \texttt{occurrence} \\
\end{longtable}
\par\begingroup\leftskip=0pt\relax\rightskip=0pt\relax\parfillskip=0pt plus 1fil\relax\vspace{2pt}\noindent\scriptsize
\emph{Legend.} Per functional relation (contract section C): each row is one distinct (support status, occurrence resolution, evidence mode) combination carried by the DerivedDecisionClaims that use a relation of a given (\texttt{source\_role}, \texttt{relation\_type}, \texttt{target\_role}, \texttt{temporal\_scope}) tuple within a case, so the three axes on a row correspond; a relation with several combinations spans several rows with its label repeated. Support status, occurrence resolution and evidence mode are three separate columns and are never merged; support status uses \texttt{supported} (not \texttt{resolved}). Domains: support status $\in$ \{\texttt{supported}, \texttt{unresolved}, \texttt{contradicted}, \texttt{n.a.}\}; occurrence resolution $\in$ \{\texttt{resolved}, \texttt{unresolved}, \texttt{n.a.}\}; evidence mode $\in$ \{\texttt{architecture}, \texttt{capability}, \texttt{procedure}, \texttt{aggregate}, \texttt{occurrence}\}. \emph{Evidence mode} maps: \texttt{architecture} $\to$ structural organization; \texttt{capability} $\to$ what the system can or is designed to do; \texttt{procedure} $\to$ specified operational sequence; \texttt{aggregate} $\to$ repetition or result without event identity; \texttt{occurrence} $\to$ bounded event-level instance. Evidence modes identify the kind of proposition supported by the source and are not treated as an ordinal scale of system quality or closure.
\par\endgroup

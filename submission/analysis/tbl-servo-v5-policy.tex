% AUTO-GENERATED by analysis/build_servo_v5_tables.py. Do not edit by hand.
%
% Table A -- Policy (per case). Suggested manuscript \caption (T11/G4 to place
% in main_post-submit.tex, table* around \input{analysis/tbl-servo-v5-policy.tex}):
%
% Policy decomposition (Table A), a schema-v3 BED-lens reading of each case's experiment-selection policy (charter B.4 / contract section B). Columns are the selection signal (free text) and five enum axes: the follow-up (control) dependence, the selection-purpose facets, and the separate candidate-selection, design-selection and candidate-execution rules. The action is $a=(h,d,P,f)$: the candidate-selection rule names which candidate hypotheses $h$ are chosen and the design-selection rule names how the experimental design/assay $d$ is chosen -- two distinct axes, so that testing every hypothesis is not read as evidence about how the design is selected. The selection-purpose facet (objective) is kept apart from all three rules, so no objective is folded into a rule -- e.g. C05, whose candidate rule is \texttt{exhaustive} / \texttt{all\_selected} (it tested every hypothesis) with a \texttt{fixed\_or\_standard\_design} / \texttt{coverage\_or\_factorial} design on the separate design axis (a model-prescribed assay with a Latin-square layout, not a discrimination-directed experiment selection), reports no uncertainty- or discrimination-directed selection objective on the bounded source and so is coded \texttt{not\_reported} on the objective axis (exhaustive testing is no experiment selection), whereas the score-directed cases (C01--C04, C06) are \texttt{performance\_improvement} with \texttt{top\_k\_ranked} / \texttt{threshold} / \texttt{sequential\_choice} candidate selection. No bounded source reports a \texttt{discrimination\_directed}, \texttt{information\_directed} or \texttt{cost\_directed} design (a corpus-level observation). generation\_scope is not shown (a provenance axis) and formal epistemic-utility evidence is \texttt{not\_reported} for all six cases (footnote).
\begingroup
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{4pt}
\let\origunderscore\_
\renewcommand{\_}{\origunderscore\allowbreak}
\begin{tabular}{l >{\raggedright\arraybackslash}p{0.18\linewidth} >{\raggedright\arraybackslash}p{0.14\linewidth} >{\raggedright\arraybackslash}p{0.16\linewidth} >{\raggedright\arraybackslash}p{0.13\linewidth} >{\raggedright\arraybackslash}p{0.14\linewidth} >{\raggedright\arraybackslash}p{0.13\linewidth}}
\toprule
Case & Selection signal & Follow-up (control) dependence & Selection-purpose facets & Candidate-selection rule & Design-selection rule & Candidate-execution rule \\
\midrule
C01 & software/execution error\newline reaction yield\newline prior-round observations\newline normalized advantage value & \texttt{failure}\newline \texttt{observation\_result} & \texttt{local\_repair}\newline \texttt{performance\_improvement} & \texttt{sequential\_choice} & \texttt{fixed\_or\_standard\_design} & \texttt{one\_at\_a\_time}\newline \texttt{until\_success} \\
C02 & execution/compilation error\newline self-assessed idea scores (interestingness/novelty/feasibility)\newline automated review scores\newline experiment results\newline literature-similarity to existing work & \texttt{failure}\newline \texttt{score}\newline \texttt{observation\_result} & \texttt{local\_repair}\newline \texttt{performance\_improvement}\newline \texttt{diversity\_directed\_selection} & \texttt{threshold} & \texttt{fixed\_or\_standard\_design} & \texttt{one\_at\_a\_time}\newline \texttt{until\_budget} \\
C03 & execution error / buggy node\newline VLM-flagged plot issues\newline LLM-evaluator performance metrics (training dynamics, plots)\newline best-performing checkpoint score\newline training-curve convergence & \texttt{failure}\newline \texttt{score}\newline \texttt{observation\_result} & \texttt{local\_repair}\newline \texttt{performance\_improvement} & \texttt{top\_k\_ranked}\newline \texttt{sampled\_subset} & \texttt{coverage\_or\_factorial} & \texttt{batch}\newline \texttt{until\_budget} \\
C04 & subtask completion (unconditional autonomous progression)\newline compile/runtime error\newline mle-solver alignment score\newline professor-agent reward-model score (0-1)\newline experiment results & \texttt{fixed\_or\_predefined}\newline \texttt{failure}\newline \texttt{score}\newline \texttt{observation\_result} & \texttt{local\_repair}\newline \texttt{performance\_improvement} & \texttt{top\_k\_ranked}\newline \texttt{sampled\_subset} & \texttt{fixed\_or\_standard\_design} & \texttt{batch}\newline \texttt{until\_success}\newline \texttt{until\_budget} \\
C05 & fixed four-step orphan-enzyme method\newline sequence similarity (PSI-BLAST/FASTA)\newline confirm/refute statistical outcome\newline model conflicts flagged for update & \texttt{fixed\_or\_predefined}\newline \texttt{score}\newline \texttt{observation\_result} & \texttt{not\_reported} & \texttt{exhaustive} & \texttt{fixed\_or\_standard\_design}\newline \texttt{coverage\_or\_factorial} & \texttt{all\_selected} \\
C06 & runtime exceptions\newline task complexity (coder-module routing)\newline assessment-agent idea score (0-10, five dimensions, weighted sum)\newline structured critiques (automated + human feedback)\newline empirical performance results & \texttt{failure}\newline \texttt{score}\newline \texttt{observation\_result} & \texttt{local\_repair}\newline \texttt{performance\_improvement} & \texttt{top\_k\_ranked} & \texttt{fixed\_or\_standard\_design} & \texttt{until\_success}\newline \texttt{until\_budget} \\
\bottomrule
\end{tabular}
\endgroup
\par\begingroup\leftskip=0pt\relax\rightskip=0pt\relax\parfillskip=0pt plus 1fil\relax\vspace{2pt}\noindent\scriptsize
\emph{Legend.} Schema-v3 BED-lens decomposition of each case's experiment-selection policy (charter B.4 / contract section B); an analytic decomposition, not a compliance label. Each cell lists the enum values the bounded source reports for that axis; a multi-valued axis is a set, not an ordering. The selection-purpose facets (the objective) are kept separate from the candidate-selection, design-selection and candidate-execution rules, so no objective is folded into a rule. The \emph{candidate-selection rule} (which candidate hypotheses $h$ are chosen) and the \emph{design-selection rule} (how the experimental design/assay $d$ is chosen) are separate axes -- the BED-central act of selecting the design $d$ that best discriminates hypotheses under a cost is distinct from choosing which $h$ to test, so an exhaustive candidate rule (e.g. C05 tests every hypothesis) is not evidence about how $d$ is selected. No bounded source reports a \texttt{discrimination\_directed}, \texttt{information\_directed} or \texttt{cost\_directed} design; the reported designs are \texttt{fixed\_or\_standard\_design} or \texttt{coverage\_or\_factorial} (a corpus-level observation, not a per-case verdict). Two policy axes are recorded in the source data but are not columns here: \emph{generation scope} (\texttt{fixed\_space} / \texttt{search\_space\_expansion} / \texttt{candidate\_diversification}) is a G/S-level provenance axis, and \emph{formal epistemic-utility evidence} is \texttt{not\_reported} for all six cases. \emph{Formal epistemic-utility evidence.} None of the bounded sources reports an explicit posterior or expected-information-gain criterion; this is a corpus-level reporting observation, not evidence of field-wide absence.
\par\endgroup
